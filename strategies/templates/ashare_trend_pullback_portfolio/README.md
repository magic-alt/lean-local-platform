# A股趋势回撤 · PIT组合

日线收盘后生成信号，使用 Market-On-Open 在下一交易日执行。技术指标使用原始 OHLC
乘以时点复权因子；成交、涨跌停判断和账户估值继续使用原始价格。

支持 `CSI300`、`CSI500`、`CSI1000`、`STAR50`，以及 A（纯趋势）、B（增加相对
强弱）、C（再增加公告时点质量）三个固定评分版本。默认市场状态控制关闭。

该模板依赖平台在运行前生成的不可变输入快照，不会在 LEAN 运行中联网获取数据。
