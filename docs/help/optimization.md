# Optimization

参数网格会展开为标准子回测，因此共享单次回测的数据预检、并发限制、取消和验证链路。

## 模式

- 单股参数网格：在一个股票上比较参数组合。
- 股票池稳健参数：在股票池中比较每组参数的分布，默认以中位Sharpe排序；有效覆盖低于80%的候选不参与最优选择。
- 动态组合参数：每组参数运行一次多资产组合。
- Walk-forward：默认三年训练、一年样本外、每年滚动；参数只在训练窗口选择。
- 多策略优化：每个策略独立寻优，再比较各自最优结果。

不要只依据最高收益选择参数。应同时观察样本外表现、最大回撤、参数邻域稳定性、失败率和交易次数。
